---
interface Props {
	date: Date;
}

const { date } = Astro.props;

---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('pt-br', {
			timeZone: "utc",
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
